id: comp_gdpr_01
name: Compliance — GDPR coverage
description: |
  Q-03 compliance benchmark: GDPR dimension.
  Verify that Argentor implements the four core GDPR data subject rights:
  Art. 17 erasure, Art. 20 portability, consent management, and DPIAs.
  Competitors score 0.0 — no GDPR module.
kind: compliance
prompt: |
  Assess GDPR compliance coverage. Check for: (1) Art. 17 right to erasure
  implementation, (2) Art. 20 data portability, (3) consent management with
  per-purpose granularity, (4) Data Protection Impact Assessment (DPIA)
  capability. Report coverage as a fraction of these four core rights.
input: ""
ground_truth: |
  gdpr_coverage=1.0 — All four core GDPR rights implemented in GdprModule:
  erasure (DataSubjectRequest::Erasure), portability (DataSubjectRequest::Portability),
  consent (ConsentStore), DPIA support in GdprModule.
simulated_turns: 1
tool_count: 0
context_size_bytes: 0
rubric:
  criteria:
    - name: erasure_and_portability
      description: Art. 17 and Art. 20 implemented.
      weight: 2.0
    - name: consent_management
      description: Per-purpose consent with granular control.
      weight: 2.0
    - name: dpia_support
      description: Data Protection Impact Assessment capability present.
      weight: 1.0
  pass_threshold: 6.0
max_turns: 1
allowed_tools: []
